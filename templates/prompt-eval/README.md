# Prompt Eval Starter

Use this starter for the Prompt Harness lab.

```bash
npx promptfoo@latest eval -c promptfooconfig.yaml
```

Replace the examples in `cases.jsonl` with real anonymized inputs before trusting any score.
